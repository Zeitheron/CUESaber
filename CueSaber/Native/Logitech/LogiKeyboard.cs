using CUESaber.Native.Logitech;

namespace CUESaber.CueSaber.Native.Logitech
{
    class LogiKeyboard
    {
		private readonly byte[] _colors = new byte[504];
        private readonly Rectangle boundaries = new Rectangle(0, 0, 21, 6);

        public static void ApplyColorGrid()
        {
        }
	}
}
